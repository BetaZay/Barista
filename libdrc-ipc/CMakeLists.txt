if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
    add_library(drc_ipc STATIC src/media_bridge.cpp)
else()
    add_library(drc_ipc STATIC src/media_bridge_stub.cpp)
endif()
target_include_directories(drc_ipc PUBLIC include)
target_compile_features(drc_ipc PUBLIC cxx_std_20)
target_link_libraries(drc_ipc PUBLIC pthread)
if(BUILD_TESTING AND PROJECT_NAME STREQUAL "drc_project" AND CMAKE_SYSTEM_NAME STREQUAL "Linux")
    add_executable(drc_ipc_tests tests/media_bridge_tests.cpp)
    target_link_libraries(drc_ipc_tests PRIVATE drc_ipc)
    add_test(NAME drc_ipc_tests COMMAND drc_ipc_tests)
endif()
