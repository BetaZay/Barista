# Cemu DRC helper build configuration for hostapd (Linux)
#
# This intentionally enables only the pieces needed for Wii U GamePad pairing
# and runtime AP hosting.

CONFIG_DRIVER_NL80211=y
CONFIG_EAP=y
CONFIG_WPS=y
CONFIG_PKCS12=y
CONFIG_IPV6=y
CONFIG_TENDONIN=y
